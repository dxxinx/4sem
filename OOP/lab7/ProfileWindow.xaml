<Window x:Class="FlowerShop.ProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:FlowerShop.ViewModels"
        Title="Личный кабинет"
        Height="300" Width="400"
        WindowStartupLocation="CenterOwner">
    <Window.DataContext>
        <vm:ProfileViewModel/>
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Имя"/>
        <TextBox Grid.Row="0" Margin="100,0,0,0"
                 Text="{Binding UserName}"/>

        <Label Grid.Row="1" Content="Email"/>
        <TextBox Grid.Row="1" Margin="100,0,0,0"
                 Text="{Binding Email}"/>

        <Label Grid.Row="2" Content="Тема"/>
        <ComboBox Grid.Row="2" Margin="100,0,0,0"
                  ItemsSource="{Binding Themes}"
                  SelectedItem="{Binding SelectedTheme}"/>

        <Label Grid.Row="3" Content="Язык"/>
        <ComboBox Grid.Row="3" Margin="100,0,0,0"
                  ItemsSource="{Binding Languages}"
                  SelectedItem="{Binding SelectedLanguage}"/>

        <Button Grid.Row="4" Content="Закрыть"
                HorizontalAlignment="Right"
                Width="100" Margin="0,10,0,0"
                Click="Button_Close_Click"/>
    </Grid>
</Window>
